#include<iostream>
#include<sstream>
#include<vector>

using namespace std;
void solver()
{ int listsz=0,callsz=0,i,j,k,dur,invalid=0,callflag=0;
  float cost,rate;
  vector<string> list,call;
  string eot="000000",eoc="#";
  while(1)
    {
      cin>>s;
      if(eot==s)
	break;
      list.push_back(s);
      listsz++;
    }
    while(1)
    {
      cin>>s;
      if(eoc==s)
	break;
      call.push_back(s);
      callsz++;
    }
    for(i=0;i<callsz;i++)
      {
	for(j=0;j<listsz;j++)
	  {

}
int main()
{
 solver();
return 0;
}

